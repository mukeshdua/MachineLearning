"""in pythin, there are only few functionalities are part of core. otherwise everything needs to be imported.
core functionlities: for loops, if statements, math operators, and some functions, like print and input
"""
from random import randint
from math import sin, pi

x = randint(1,10)
print(x)

print('Pi is roughly', pi)
print('sin(0) =', sin(0))
print(abs(-4.3))
print(round(3.336, 2))
print(round(345.2, -1))

#get help
# go to python shell by typing "python", import(math), dir(math)
